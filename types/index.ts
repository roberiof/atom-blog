export interface PostType {
    id: number;
    date: string;
    title: string;
    image: string;
    description: string;
}

export interface MessageType {
    name: string;
    email: string;
    message: string;
}